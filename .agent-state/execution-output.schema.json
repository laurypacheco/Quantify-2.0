{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "execution-output",
  "description": "Resultado de una ejecución de test generado por el executor agent. Input para debugger o reporter.",
  "type": "object",
  "required": ["tc_id", "spec", "result"],
  "properties": {
    "tc_id": { "type": "integer" },
    "spec": { "type": "string" },
    "result": { "type": "string", "enum": ["pass", "fail", "timeout"] },
    "duration_ms": { "type": ["integer", "null"] },
    "terminal_output": { "type": "string", "description": "Output completo del terminal de npx playwright test" },
    "screenshots": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Rutas a screenshots capturados durante la ejecución"
    },
    "error_summary": {
      "type": ["string", "null"],
      "description": "Resumen legible del error. null si pasó."
    },
    "timestamp": { "type": "string", "format": "date-time" }
  }
}
